# RecEngine
Build a product recommendation API with three model layers: collaborative filtering (Model A), LLM-enhanced semantic recommendations (Model B), and a supervised CTR prediction model (Model C) that learns the optimal blend of both from real A/B test feedback. 
